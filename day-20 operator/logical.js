let age =  true

let hasLicense = false

if(age && hasLicense)
{
     console.log("you can drive");
} // and &&


if(age || hasLicense)
{
     console.log("you can drive");
} // or ||


if(!hasLicense)
{
     console.log("you can drive");
} // not !